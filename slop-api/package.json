{
  "name": "slop-api",
  "version": "1.0.0",
  "description": "Slop Generator API — serves app ideas as structured JSON over HTTPS with JWT auth",
  "type": "module",
  "scripts": {
    "start": "node scripts/api-server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vitest": "^3.1.1"
  }
}
