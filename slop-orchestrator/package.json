{
  "name": "slop-orchestrator",
  "version": "1.0.0",
  "description": "Load controller — coordinates slop-planner and slop-builder turn-based batch execution",
  "type": "module",
  "scripts": {
    "start": "node scripts/orchestrator.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.3.0",
    "express": "^4.21.0",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2",
    "vitest": "^3.1.1"
  }
}
