{
  "name": "slop-builder",
  "version": "1.0.0",
  "description": "Autonomous App Builder Agent — consumes ideas from slop-api, builds production apps",
  "type": "module",
  "scripts": {
    "start": "node scripts/agent-runner.js",
    "dev": "node scripts/agent-runner.js --debug",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.0",
    "form-data": "^4.0.0",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
